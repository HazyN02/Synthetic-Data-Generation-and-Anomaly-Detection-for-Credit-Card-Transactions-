# Core
pandas>=1.3
pyarrow>=10.0  # parquet support (required for train_merged.parquet)
numpy>=1.20
scikit-learn>=1.0
scipy>=1.7  # for statistical_tests (paired t-test, permutation)
lightgbm>=3.0

# Oversampling
imbalanced-learn>=0.10

# Generative models (CTGAN, TabDDPM)
ctgan>=0.7.0
torch>=1.10

# Paper figures
matplotlib>=3.5
